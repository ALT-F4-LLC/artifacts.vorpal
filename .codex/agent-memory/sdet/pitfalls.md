- Symptom: `script/test-detect-changed-artifacts.sh` fails on macOS with `declare: -A: invalid option` after the first few checks. -> Root cause: the test invokes `script/detect-changed-artifacts.sh` through the default GNU Bash 3.2, but the script uses associative arrays. -> Resolution: run the harness with Bash 4+ when verifying locally, or report the Bash 3.2 compatibility blocker rather than treating zlib discovery as failed.
